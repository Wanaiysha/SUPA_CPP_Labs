// Wan Aishah Wan Harun
// Preliminary Questions

#include <iostream>
#include <cmath> // Use cmath instead of math.h for C++

// Function definition
double resultantVector(double x_val, double y_val) {
    return sqrt((x_val * x_val) + (y_val * y_val));
}
